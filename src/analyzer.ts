/**
 * Schema Analyzer
 *
 * Analyzes a GraphQL schema to extract types, relationships, and characteristics
 * that inform caching decisions.
 *
 * @module analyzer
 */

export { analyzeSchema, generateSchemaSummary } from "./analyzer/index.js";
